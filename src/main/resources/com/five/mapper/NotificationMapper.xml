<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.five.mapper.NotificationMapper">

    <resultMap id="NotifMap" type="com.five.entity.Notification">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="type" column="type"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="relatedId" column="related_id"/>
        <result property="relatedType" column="related_type"/>
        <result property="isRead" column="is_read"/>
        <result property="createTime" column="create_time"/>
    </resultMap>
<!-- 插入通知-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notification(user_id, type, title, content, related_id, related_type, is_read, create_time)
        VALUES(#{userId}, #{type}, #{title}, #{content}, #{relatedId}, #{relatedType}, 0, NOW())
    </insert>
<!--统计未读通知数量-->
    <select id="countUnreadByUserId" resultType="int">
        SELECT COUNT(*) FROM notification WHERE user_id = #{userId} AND is_read = 0
    </select>
<!--分页查询通知列表-->
    <select id="listByUserId" resultMap="NotifMap">
        SELECT * FROM notification
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
        LIMIT #{offset}, #{size}
    </select>
<!--标记单条通知为已读-->
    <update id="markAsRead">
        UPDATE notification SET is_read = 1 WHERE id = #{id}
    </update>
<!--全部标记为已读-->
    <update id="markAllAsRead">
        UPDATE notification SET is_read = 1 WHERE user_id = #{userId} AND is_read = 0
    </update>

</mapper>
